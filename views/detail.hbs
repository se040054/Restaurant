<div class="detail-container">
  <h1>{{restaurant.name}}</h1>
  <h2>{{restaurant.name_en}}</h2>
  <img src="{{restaurant.image}}" alt="">
  <p>分類 : {{restaurant.category}}</p>
  <p>地址 : {{restaurant.location}}</p>
  <a href="{{restaurant.google_map}}">{{restaurant.google_map}}</a>
  <p>電話 : {{restaurant.phone}}</p>
  
  <p>評分 : {{restaurant.rating}}</p>
  <p>{{restaurant.description}}</p>
<a href="/restaurants/{{restaurant.id}}/edit"><button>Edit</button> </a>
          <form action="/restaurants/{{restaurant.id}}?_method=DELETE" method="POST">
            <button type="submit">Delete</button>
          </form>
</div>

<a href="/restaurants" class="back"> ↩️</a>

